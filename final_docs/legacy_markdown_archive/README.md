# Legacy Markdown Archive

This folder stores previous documentation trees that were moved from the repository root:

1. `documentation/`
2. `documents/`
3. `onboarding_docs/`

Reason:
- keep historical markdown available
- remove duplication from top-level navigation
- make `final_docs/` the primary documentation entry point

Use `../09_Legacy_Markdown_Mapping.md` to find where each older topic is consolidated in the new docs.
